interface:
  display_name: "Audio Transcribe Notes"
  short_description: "Whisper transcripts to Markdown notes"
  default_prompt: "Use $audio-transcribe-notes to transcribe this recording and generate structured Markdown notes."
policy:
  allow_implicit_invocation: true
